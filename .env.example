# Keep disabled until the app, Stripe, expiry and refund terms are verified.
COMPLETE_OFFER_CHECKOUT_ENABLED=false
# Private, durable submission API. No credentials belong in NEXT_PUBLIC variables.
# Success requires a committed record: id, created_at, status: pending, stored: true.
PARTNER_SUBMISSION_URL=
PARTNER_SUBMISSION_TOKEN=
